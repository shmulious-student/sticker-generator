// Copy this file to `Secrets.xcconfig` (same folder) and fill in your keys.
// `Secrets.xcconfig` is git-ignored so your keys never get committed.
//
// IMPORTANT: xcconfig treats `//` as a comment. API keys generally don't contain
// `//`, but if one ever does, it will be truncated — keep that in mind.

GEMINI_API_KEY =
HUGGINGFACE_API_KEY =
REPLICATE_API_KEY =
